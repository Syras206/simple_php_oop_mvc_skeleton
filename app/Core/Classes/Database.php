<?php
namespace App;

if (!defined('APP')) { exit; }

use PDO;

/**
 * Database Class.
 */
class Database {

	/**
	 * Set everything up for our connection
	 * @access public
	 * @param  array  $options (default: [])
	 */
	function __construct(array $options = []) {
		//
	}

}